### How to format the write command/function

example of write "putchar"

(write(output type, &c, size);

output types:
0: standard input
1: standard output
2: standard error

&c: address of the buffer, explaining what type of variable it would be

size: the size in bytes of the buffer, doesn't have to be only one byte
